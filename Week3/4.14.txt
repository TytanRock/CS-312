The proof is wrong because it assumed that a subset of expressions defined by a*b* is nonregular. A regular language can contain nonregular expressions, and the proof only showed that the subset of expressions that requires the same number of 0's and 1's is nonregular.
